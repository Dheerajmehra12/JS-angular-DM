import { NetworkInterceptor } from './network-interceptor';

describe('NetworkInterceptor', () => {
  it('should create an instance', () => {
    expect(new NetworkInterceptor()).toBeTruthy();
  });
});
